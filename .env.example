# =============================================================================
# Agency-26 Hackathon — per-team app deployment config
# Copy this to `.env` (gitignored) and fill in the values your organizer gave you.
# Used by deploy-app.ps1 to build + deploy your Container Apps.
# =============================================================================

# --- Azure platform (provided by organizer) ---------------------------------
AZURE_SUBSCRIPTION_ID=a615e5c1-b4d4-4d6f-93bd-adc740cbb5d8
AZURE_PREFIX=hack26test
PLATFORM_RG=rg-hack26test-platform
TEAM_RG=rg-hack26test-team-1
ACR_NAME=hack26testacr2gevy5blbh6c6
ACR_LOGIN_SERVER=hack26testacr2gevy5blbh6c6.azurecr.io
CONTAINER_APPS_ENV=cae-hack26test
KEY_VAULT_NAME=kv-hack26test-2gevy5
APIM_GATEWAY_URL=https://apim-hack26test.azure-api.net

# --- Per-team APIM subscription key (organizer hands one to each team) -------
APIM_SUBSCRIPTION_KEY=

# --- Render Postgres (read-only replica, paste your connection string) ------
# Format: postgresql://user:pass@host/dbname
DB_CONNECTION_STRING=

# --- Optional: extra LLM providers ------------------------------------------
ANTHROPIC_API_KEY=
GOOGLE_APPLICATION_CREDENTIALS_JSON=

# --- App settings (defaults are fine) ---------------------------------------
DASHBOARD_APP_NAME=ca-hack26test-dashboard
DOSSIER_APP_NAME=ca-hack26test-dossier
APP_IMAGE_TAG=latest
